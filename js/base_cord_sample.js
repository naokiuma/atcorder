// 基本コード
// inputに入力データ全体が入る
function Main(input) {
	// input = input.split(' ');
	input = input.split('\n');
	console.log(input)
	console.log("です")
}
//*この行以降は編集しないでください（標準入出力から一度に読み込み、Mainを呼び出します）
// Main(require("fs").readFileSync("/dev/stdin", "utf8"));


//実行
// Main(`a
// b c
// d`);

//実行方法
// node このファイルでok